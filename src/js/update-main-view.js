const messages = {
    HOMEPAGE: 'homepage',
    BLOG: 'blog'
};

function update(msg, model) {
    switch (msg) {
        case messages.HOMEPAGE:
            // code
            break;
        
        default:
            // code
    }
}